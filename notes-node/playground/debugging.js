var person = {
    name: 'Kyle'
};

person.age = 27;

// node inspect
// c to continue
// n to go to the next line
// repl to enter into interactive terminal
debugger;

person.name = 'Mike';

console.log(person);